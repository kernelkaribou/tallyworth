# Copy this file to .env and set a strong, random value.
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Currency symbol shown in the UI (single currency for alpha).
CURRENCY_SYMBOL=$
